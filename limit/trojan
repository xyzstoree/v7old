#!/bin/bash
# limit/trojan — daemon kuota trojan
LIB="$(dirname "$(readlink -f "$0")")/_limit_proto.lib"
[ -f "$LIB" ] && . "$LIB" || . /etc/xray/_limit_proto.lib
run_protocol_loop "#!" trojan /etc/trojan/.trojan.db
