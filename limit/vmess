#!/bin/bash
# limit/vmess — daemon kuota vmess
# FIX #5/#37: restart xray hanya ketika ada user expired, sleep 60s (was 30s/5s)
LIB="$(dirname "$(readlink -f "$0")")/_limit_proto.lib"
[ -f "$LIB" ] && . "$LIB" || . /etc/xray/_limit_proto.lib
run_protocol_loop "###" vmess /etc/vmess/.vmess.db
