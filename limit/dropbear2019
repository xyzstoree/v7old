#!/bin/bash
# dropbear2019 — wrapper untuk pasang Dropbear 2019.78 (versi yang sama dengan ganti-versi-db).
# Gunakan ganti-versi-db sebagai backend tunggal agar tidak duplikasi logika.
exec /projects/dropbear-installer 2>/dev/null || exec bash <(curl -sSL "https://raw.githubusercontent.com/xyzstoree/v7old/main/limit/ganti-versi-db") "$@"
