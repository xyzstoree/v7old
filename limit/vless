#!/bin/bash
# limit/vless — daemon kuota vless
LIB="$(dirname "$(readlink -f "$0")")/_limit_proto.lib"
[ -f "$LIB" ] && . "$LIB" || . /etc/xray/_limit_proto.lib
run_protocol_loop "#&" vless /etc/vless/.vless.db
